module m3.strings{
    export var CanvasPointerMismatchErrorMessage = `previous pointer should never be the
    same as current, forgot a begin()?`;

    export var ExperimentalFeaturesOnMessage = `Warning: Experimental features is turned on
                             & might(probably will) result in rendering
                             inconsistencies.`;
}